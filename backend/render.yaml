services:
  - name: ai-post-backend
    type: web
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: GROQ_API_KEY
        sync: false
